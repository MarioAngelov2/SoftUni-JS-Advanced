function solve(input) {

    let res = [];

    for (let card of input) {
        let face = card.slice(0, -1)
        let back = card.slice(-1)
        
        console.log(face)
    }
}
solve(['AS', '10D', 'KH', '2C'])